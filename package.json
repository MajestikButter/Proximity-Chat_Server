{
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/ws": "^8.5.3"
  },
  "scripts": {
    "run": "node --enable-source-maps dist/index.js",
    "start": "npm run run",
    "build": "node build-to-run.js"
  },
  "dependencies": {
    "websocket-stream": "^5.5.2",
    "esbuild": "^0.14.48",
    "ws": "^8.8.0"
  }
}
